/*!
 * Sticky-kit
 * A jQuery plugin for making smart sticky elements
 *
 * Source: http://leafo.net/sticky-kit/
 */

$("#summer-character").stick_in_parent({
  // offset_top: 10,
  // offset_left: 100,

});

$("#spring-character").stick_in_parent({
  // offset_top: 10
  // spacer: false
});

$("#fall-character").stick_in_parent({
  // offset_top: 10
});

$("#winter-character").stick_in_parent({
  // offset_top: 10
});
